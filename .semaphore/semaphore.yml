version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
  
# containers:
#   - name: postgres
#     image: postgres:10-alpine
#     env_vars:
#       - name: POSTGRES_PASSWORD
#         value: 3x4mpl3P455w0rd

blocks:
  - name: "Build"
    task:
      env_vars:
        - name: APP_ENV
          value: prod
      jobs:
      - name: Docker build
        commands:
          - checkout
          - "docker build --pull --rm 
            --tag ember-fastboot-demo-backend:testing-build-${SEMAPHORE_BUILD_NUMBER:-local}
            --target testing
            ./backend"
  